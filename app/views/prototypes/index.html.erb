<main class="main">
  <div class="inner">
    <% if user_signed_in? %>
      <%# ログインしているときは以下を表示する %>
      <div class="greeting">
        
        <%= link_to "こんにちは、#{current_user.name}さん", root_path, class: :greeting__link %>
      </div>
      <%# ログインしているときは上記を表示する %>
    <% else %>
      <div class="card__wrapper">
        <%# 投稿機能実装後、部分テンプレートでプロトタイプ投稿一覧を表示する %>
      </div>
    <% end %>
  </div>
</main>
